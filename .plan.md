# План: v1.4.0 в ветке dev — 9 фич (выполнен, не зарелижен)

## Цель
Ветка `dev`, коммит на фичу, main не трогаем до готовности.

## Фичи и порядок (от простого к сложному)

1. [x] **Счётчик элементов** в заголовке колонки → verify: build
2. [x] **Копировать markdown-ссылку + Obsidian URL** в меню файла
       (`fileManager.generateMarkdownLink`, `obsidian://open?vault=&file=`) → verify: build
3. [x] **Меню заголовка колонки** (правый клик): новая заметка/папка/холст → verify: build
4. [x] **Сортировка per-folder** — `columnSortModes: Record<path, SortMode>`,
       пункты сортировки в меню заголовка (галочка у активной), remap/prune,
       toolbar-кнопка остаётся глобальным дефолтом → verify: тест на выбор сортировки, build
5. [x] **Миниатюры картинок в grid** — в grid-виде у image-файлов `<img loading="lazy">`
       через `vault.getResourcePath` вместо иконки → verify: build, руками в vault
6. [x] **Превью PDF/аудио/видео** в колонке превью — `<audio>/<video controls>`,
       PDF через `<iframe>` (desktop) → verify: build, руками
7. [x] **Folder notes** — выбор папки дополнительно открывает заметку
       `<папка>/<имя папки>.md`, если существует; toggle в настройках (default off);
       подчёркивание имени папки с заметкой → verify: тест pure-хелпера пути, build
8. [x] **Иконки папок** — контекст-меню папки → «Иконка папки», FuzzySuggestModal
       по `getIconIds()` с превью, «Стандартная» для сброса;
       `folderIcons: Record<path, string>`, remap/prune → verify: build, руками
9. [x] **Порядок pinned перетаскиванием** — `pinnedPaths: Record<path, true>` →
       `Record<path, number>` (порядок, миграция в loadSettings: true → номер);
       drop pinned-элемента на pinned-элемент той же папки = reorder, не move
       → verify: тесты pinnedFirst с порядком + reorder-хелпер, build
10. [x] **Declarative settings** (`getSettingDefinitions()`, Obsidian 1.13) —
       добавить ПАРАЛЛЕЛЬНО display(): старые версии используют display(),
       1.13+ получает поиск по настройкам. Если lint потребует minAppVersion 1.13 — скипаем
       → verify: lint 0 errors, build

## Финал
11. [x] i18n en/ru для всего, eslint+tsc+тесты+build, README (фичи), версия 1.4.0
        в dev БЕЗ тега (релиз после мерджа в main, когда пройдёт ревью 1.3.3)

## Допущения
- Folder note формат: `Folder/Folder.md` (как плагин Folder notes по умолчанию)
- Пины: reorder только внутри одной папки
- PDF-превью: если iframe не рендерит на мобиле — просто иконка (isDesktopOnly не меняем)

## Риски
- №9 меняет тип pinnedPaths — нужна миграция старых данных (true → номер)
- №10 может конфликтовать с no-unsupported-api — тогда скип, main-функционал не страдает

# План: v1.3.2 — фикс скролла, pin, свотчи цвета, новый холст (выполнен)

## Цель
1. Клик по заметке не сбрасывает вертикальный скролл колонок
2. Pin: закрепить файл/папку сверху колонки (правый клик → Закрепить)
3. Цвет папки в меню виден визуально (кружок + сам текст пункта покрашен)
4. Правый клик по пустому месту → пункт «Новый холст» (canvas)

## Допущения
- «новый золст» = «новый холст» (canvas)
- Кружок цвета уже был в коде v1.3.0; если его не видно в Obsidian —
  в vault старая сборка (обновить main.js/styles.css и перезагрузить плагин)

## Шаги
1. [x] Скролл: render() сохраняет/восстанавливает scrollTop колонок по data-folder-path;
       scrollLeft восстанавливается, если набор колонок не изменился; то же в doRefresh()
       → verify: build ок
2. [x] Pin: pinnedPaths в настройках; pinnedFirst (pure, TDD); пункт меню
       Закрепить/Открепить; иконка pin; remap/prune при rename/delete
       → verify: npm test 24 passed
3. [x] Свотчи: текст пункта меню цвета покрашен цветом папки
       → verify: build ок
4. [x] «Новый холст» в showFolderBackgroundMenu; createNote(folder, ext, content)
       → verify: build ок
5. [x] eslint + tsc + tests + build зелёные, версия 1.3.2
       → verify: всё зелёное

## Не делаем
- «Новый холст» в тулбаре и в меню папки (только пустое место)
- Перетаскивание порядка закреплённых

## Риски
- setSubmenu приватный API (уже используется) — при пропаже пункты лягут плоско
